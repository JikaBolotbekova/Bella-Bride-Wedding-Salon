apiVersion: v1
kind: Service
metadata:
  name: {{ include "Bella-Bride-Wedding-Salon-helm.fullname" . }}
  labels:
    {{- include "Bella-Bride-Wedding-Salon-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Bella-Bride-Wedding-Salon-helm.selectorLabels" . | nindent 4 }}
