apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Bella-Bride-Wedding-Salon-helm.fullname" . }}-test-connection"
  labels:
    {{- include "Bella-Bride-Wedding-Salon-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Bella-Bride-Wedding-Salon-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
